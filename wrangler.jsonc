/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
	"$schema": "node_modules/wrangler/config-schema.json",
	"name": "enjoy-web-app",
	"compatibility_date": "2025-09-02",
	"compatibility_flags": [
		"nodejs_compat"
	],
	"main": "./src/server/index.ts",
	"observability": {
		"enabled": true
	},
	"assets": {
		"directory": "./dist/client",
		"binding": "ASSETS",
		"run_worker_first": [
			"/api/*"
		]
	},
	/**
	 * KV Namespace Bindings
	 * Used for rate limiting and other key-value storage needs
	 *
	 * To create a KV namespace:
	 * 1. Run: wrangler kv:namespace create "RATE_LIMIT_KV"
	 * 2. This will output a namespace ID, add it to the "id" field below
	 * 3. For preview environments, also create: wrangler kv:namespace create "RATE_LIMIT_KV" --preview
	 *
	 * For translation cache:
	 * 1. Run: wrangler kv:namespace create "TRANSLATION_CACHE_KV"
	 * 2. This will output a namespace ID, add it to the "id" field below
	 * 3. For preview environments, also create: wrangler kv:namespace create "TRANSLATION_CACHE_KV" --preview
	 */
	"kv_namespaces": [
		{
			"binding": "RATE_LIMIT_KV",
			"id": "950d90c909264f40a6fbad68469eea91",
			"preview_id": "299e6a5c12fd4ea5888a4cf23b96ab4a"
		},
		{
			"binding": "TRANSLATION_CACHE_KV",
			"id": "c98f68336b8c47de91699ec88e79eb6f",
			"preview_id": "389491b5871e43e78d1257bd5cf31350"
		}
	],
	/**
	 * Workers AI Binding
	 * Required for OpenAI-compatible endpoints using Cloudflare Workers AI
	 */
	"ai": {
		"binding": "AI"
	},
	/**
	 * Environment Variables (Non-sensitive)
	 * For sensitive data like API keys, use secrets instead (see below)
	 *
	 * These can be overridden per environment using --env flag
	 */
	"vars": {
		"AZURE_SPEECH_REGION": "southeastasia",
		"API_BASE_URL": "https://enjoy.bot",
		"WORKERS_AI_TEXT_MODEL": "@cf/meta/llama-3.1-8b-instruct-fast",
		"WORKERS_AI_TTS_MODEL": "@cf/myshell-ai/melotts",
		"WORKERS_AI_ASR_MODEL": "@cf/openai/whisper-large-v3-turbo"
	},
	/**
	 * Secrets (Sensitive Data)
	 *
	 * DO NOT put secrets in this file!
	 * Use one of these methods instead:
	 *
	 * 1. For production/staging:
	 *    wrangler secret put AZURE_SPEECH_SUBSCRIPTION_KEY
	 *
	 * 2. For local development:
	 *    Create a .dev.vars file (see .dev.vars.example)
	 *    This file is automatically loaded by wrangler dev
	 *
	 * Required secrets:
	 * - AZURE_SPEECH_SUBSCRIPTION_KEY: Azure Speech Service subscription key
	 *
	 * Optional secrets:
	 * - (Add other secrets as needed)
	 */
	/**
	 * Environment-specific configurations
	 * Use --env flag to switch between environments
	 */
	// "env": {
	//   "production": {
	//     "vars": {
	//       "API_BASE_URL": "https://enjoy.bot"
	//     }
	//   },
	//   "staging": {
	//     "vars": {
	//       "API_BASE_URL": "https://staging.enjoy.bot"
	//     }
	//   }
	// }
	/**
	 * Smart Placement
	 * Docs: https://developers.cloudflare.com/workers/configuration/smart-placement/#smart-placement
	 */
	// "placement": { "mode": "smart" }
	/**
	 * Service Bindings (communicate between multiple Workers)
	 * https://developers.cloudflare.com/workers/wrangler/configuration/#service-bindings
	 */
	// "services": [{ "binding": "MY_SERVICE", "service": "my-service" }]
}