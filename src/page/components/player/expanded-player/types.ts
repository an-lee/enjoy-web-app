/**
 * ExpandedPlayer component props
 *
 * Note: Media loading and playback logic is now managed internally by ExpandedPlayer
 * to reduce prop drilling. Only styling props are exposed.
 */
export interface ExpandedPlayerProps {
  /** Optional className for styling */
  className?: string
}

